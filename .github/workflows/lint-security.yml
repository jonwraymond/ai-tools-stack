name: Lint & Security

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:

permissions:
  contents: read

jobs:
  lint-security:
    runs-on: ubuntu-latest
    env:
      GOPRIVATE: github.com/jonwraymond/*
      GONOSUMDB: github.com/jonwraymond/*
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Download modules
        run: go mod download

      - name: golangci-lint (revive enabled)
        uses: golangci/golangci-lint-action@v7
        with:
          version: v2.8.0
          args: --enable revive

      - name: Gosec
        run: |
          go install github.com/securego/gosec/v2/cmd/gosec@v2.22.11
          "$(go env GOPATH)/bin/gosec" ./...
